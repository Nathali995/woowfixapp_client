import {StyleSheet} from 'react-native'

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: 'white',
    paddingVertical: 20,
  },
  titleContainer: {
    fontSize: 14,
    marginVertical: 15,
    alignContent: 'center',
    alignItems: 'center',
  },
})
